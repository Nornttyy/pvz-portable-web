Mode: built-in image_gen edit. Final corrected prompt, after rejecting the initial two-row drafts (wrong row edited; opaque backgrounds).

Use case: precise-object-edit. Create a PRODUCTION TRANSPARENT PNG skeletal game sprite strip. Input images 1, 2, 3 are actual original intact, partially damaged, heavily damaged helmet pieces. Image 4 is the custom helmet identity to be redrawn in those three states. Preserve the ORIGINAL 2009 Plants vs Zombies soft low-resolution cartoon rendering, brown outlines, muted colors, simple broad shading, original three-quarter perspective and bottom attachment. Draw EXACTLY THREE isolated pieces in ONE HORIZONTAL ROW: the custom helmet from image 4 intact; same custom helmet lightly damaged; same custom helmet heavily damaged. Do NOT include any ordinary reference helmet or second row. Do not include any whole zombie or head. The output must have actual alpha transparency: zero backdrop, no glow, no gradients, no scenery, no shadow, no checkerboard, no labels. Each part centered in one equal-width third of the canvas, clear margins. Keep the lower rim attachment and tilt constant as the upper edge becomes more crumpled or torn. No glossy metal, no photographic textures, no vector redesign. All three are the SAME custom item with increasing damage. Identity:

Append the variant identity from zombie-armor-redraw.json. Inputs: reanim/<native 1..3> and parts/<key>-prop.png.
